// import action variables

const initialState = {
    friends: [],
    error: '',
    pending: false
}

export const friendsList = (state = initialState, action) => {
    switch(action.type) {

        default:
            return state;
    }
}